# 📌 日期格式

该文档总结主流的日期格式。寻求经典场景下的优秀方案

## 📄 问题与场景

**问题**

1. 日期格式多种多样，带来了转换问题
2. 时间本身信息丰富，细节繁多。（比如时区、合法性、表达形式等）越注意细节，维护越麻烦

**场景**

1. 多端协调开发：客户端、硬件端、前端传时间
   - 统筹一种时间格式

2. 后端接收到之后，对时间做管理

   - 保持什么格式 + 何时做格式转换

   - 时间转换器维护

   - 数据库存储以及细节

## 📄 格式规范

### 🔖 ISO 8601 / RFC 3339 （文本标准）

>  这是目前 RESTful API、JSON 数据交换、以及大多数现代数据库交互的**首选标准**。

- **格式：** `2023-12-30T15:30:00Z` 或 `2023-12-30T15:30:00+08:00`
  -  **T 分隔符：** 日期和时间中间用 T 分隔
  - **时区明确：** 结尾的 Z 代表 UTC（世界标准时间），或者使用 +08:00 明确指定偏移量。
  - **排序友好：** 字符串的字典序即为时间顺序。
- **生态：**
  - Google APIS
  - AWS APIS
  - Kubernets
  - JavaScript
  - Java

### 🔖 Unix Timestamp （数值标准）

>  这是移动端应用、高频交易系统、内部微服务调用的主流选择。

- **格式：** `1703921400 (秒级)` 或 `1703921400000 (毫秒级)`
  - 极致轻量：整数 long，占用字节数极少
  - 跨语言
  - 无时区歧义：永远代表距离 1970-01-01 00:00:00 UTC 的毫秒数。
- **生态：**
  - 微信/支付宝支付接口
  - Prometheus/Grafana：监控系统底层存储
  - Redis：内部过期时间

### 🔖  传统协议标准：RFC 5322 / RFC 1123 (HTTP/Email)

- **格式：** ` Tue, 30 Dec 2025 15:30:00 GMT`
- **生态：**
  - 浏览器缓存：HTTP Header 中的 Last-Modified, Date, Expires
  - Cookie：过期时间

## 📄 工具集

语言内部、或生态上一般集成了好用的日期框架。
团队一般也会开发一个内部的工具类，调用相关框架接口。其目的一般为：

- 约束开发者，使用统一规范
- 作为适配器，对多种日期格式做兼容

如果自己开发一个工具，要避免脱裤子放💨 的处境

### 🔖 Java

${\large {\color[RGB]{250, 157, 30}发展阶段}} $

1. 黑暗时代：`JDK 1.0 - java.util.Date`

   - 设计混乱：名叫 Date，却包含时间
   - 可变性（Mutable）线程不安全
   - 反人类设计：月份从 0 开始，年份从 1900 开始计数
   - API 复杂，无法处理国际化

2. 补丁时代：`JDK 1.1-java.util.Calendar`

   - 线程不安全
   - API 繁琐，性能差

3. 社区救世主 `Joda-Time`

   - 线程安全（不可变）、逻辑清晰、API 优美

4. 官方标准化 `JDK 8+ - java.time`

   官方以 `Joda-Time` 为原型，置顶了 JSR-310 规范，在 Java8 中正式内置.

${\large {\color[RGB]{250, 157, 30}核心类介绍}} $

当然可以！以下是 `java.time` 核心类的简明表格汇总：

| 类名                | 用途说明                            | 是否含时区 | 示例场景               |
| ------------------- | ----------------------------------- | ---------- | ---------------------- |
| `LocalDate`         | 仅表示日期（年-月-日）              | ❌          | 生日、节假日           |
| `LocalTime`         | 仅表示时间（时:分:秒.纳秒）         | ❌          | 营业时间、闹钟         |
| `LocalDateTime`     | 表示本地日期和时间                  | ❌          | 日志记录、会议安排     |
| `ZonedDateTime`     | 带时区的完整日期时间                | ✅          | 国际航班、跨时区应用   |
| `Instant`           | 时间戳（UTC，自1970-01-01起的纳秒） | ✅（UTC）   | 系统事件、性能计时     |
| `Duration`          | 两个时间点之间的时间量（秒/纳秒）   | —          | 计算耗时、倒计时       |
| `Period`            | 两个日期之间的日期量（年/月/日）    | —          | 年龄计算、合同期限     |
| `ZoneId`            | 时区标识（如 `"Asia/Shanghai"`）    | —          | 设置或转换时区         |
| `ZoneOffset`        | UTC 偏移量（如 `+08:00`）           | —          | 精确偏移处理           |
| `DateTimeFormatter` | 日期时间格式化与解析工具            | —          | 字符串 ↔ 日期时间 转换 |

${\large {\color[RGB]{250, 157, 30}demo 类}} $

```java
import java.time.*;
import java.time.format.DateTimeFormatter;
import java.time.temporal.ChronoUnit;

/**
 * java.time 核心场景示例工具类（包含详细注释）
 */
public class JavaTimeDemoUtils {

    /**
     * 获取当前本地日期、时间、日期时间
     * - 遵循 ISO-8601 标准，默认输出格式：
     *   LocalDate: 2025-12-31
     *   LocalTime: 23:59:59.123456789
     *   LocalDateTime: 2025-12-31T23:59:59.123
     * - 数据库存储：DATE, TIME, DATETIME
     * - 前端展示：直接使用默认输出或通过 DateTimeFormatter 自定义格式
     */
    public static void demoLocalTypes() {
        System.out.println("LocalDate: " + LocalDate.now());
        System.out.println("LocalTime: " + LocalTime.now());
        System.out.println("LocalDateTime: " + LocalDateTime.now());
    }

    /**
     * 创建指定日期/时间
     * - 输出格式同上
     * - 数据库：直接存储对应类型
     * - 前端：建议使用 ISO 格式传输
     */
    public static void demoConstruction() {
        LocalDate date = LocalDate.of(2025, 12, 31);
        LocalTime time = LocalTime.of(23, 59, 59);
        LocalDateTime dateTime = LocalDateTime.of(date, time);

        System.out.println("Constructed LocalDateTime: " + dateTime);
    }

    /**
     * 带时区的时间（ZonedDateTime）
     * - 默认输出格式：2025-12-31T23:59:59.123+08:00[Asia/Shanghai]
     * - 数据库：TIMESTAMPTZ (PostgreSQL), TIMESTAMP (MySQL)
     * - 前端：建议去除时区 ID [Asia/Shanghai] 只保留偏移量 +08:00 或转换为 UTC
     */
    public static void demoZonedDateTime() {
        ZonedDateTime zdt = ZonedDateTime.now(ZoneId.of("Asia/Shanghai"));
        System.out.println("ZonedDateTime (Shanghai): " + zdt);

        ZonedDateTime utc = zdt.withZoneSameInstant(ZoneOffset.UTC);
        System.out.println("Converted to UTC: " + utc);
    }

    /**
     * 时间戳（Instant）与 Date 互转
     * - 输出格式：2025-12-31T15:59:59.123Z (UTC)
     * - 数据库：TIMESTAMP 或 BIGINT (毫秒数)
     * - 前端：通常作为 ISO 字符串传输
     */
    public static void demoInstantAndDate() {
        Instant now = Instant.now();
        System.out.println("Instant: " + now);

        // Instant → Date
        java.util.Date date = Date.from(now);
        // Date → Instant
        Instant back = date.toInstant();

        System.out.println("Date from Instant: " + date);
        System.out.println("Back to Instant: " + back);
    }

    /**
     * 计算时间差：Duration（基于时间）
     * - 输出格式：以小时、分钟等单位表示
     * - 数据库：不直接存储，计算结果可用于业务逻辑
     * - 前端：可显示为“X小时Y分钟”
     */
    public static void demoDuration() {
        LocalTime start = LocalTime.of(9, 0);
        LocalTime end = LocalTime.of(17, 30);
        Duration duration = Duration.between(start, end);

        System.out.println("Work duration: " + duration.toHours() + "h " + duration.toMinutesPart() + "m");
    }

    /**
     * 计算日期差：Period（基于日历）
     * - 输出格式：年、月、日
     * - 数据库：不直接存储，计算结果可用于业务逻辑
     * - 前端：显示为年龄、项目周期等
     */
    public static void demoPeriod() {
        LocalDate birth = LocalDate.of(1990, 5, 15);
        LocalDate today = LocalDate.now();
        Period age = Period.between(birth, today);

        System.out.println("Age: " + age.getYears() + " years, " + age.getMonths() + " months");
    }

    /**
     * 格式化与解析（线程安全）
     * - 输出格式：根据自定义模式，例如 "yyyy-MM-dd HH:mm:ss"
     * - 数据库：用于插入前格式化或查询后解析
     * - 前端：确保前后端格式一致
     */
    public static void demoFormatting() {
        DateTimeFormatter formatter = DateTimeFormatter.ofPattern("yyyy-MM-dd HH:mm:ss");

        LocalDateTime now = LocalDateTime.now();
        String text = now.format(formatter);
        LocalDateTime parsed = LocalDateTime.parse(text, formatter);

        System.out.println("Formatted: " + text);
        System.out.println("Parsed back: " + parsed);
    }

    /**
     * 日期时间加减操作
     * - 输出格式：同 LocalDateTime 默认格式
     * - 数据库：无需特殊处理，直接更新相应字段
     * - 前端：根据需求调整显示格式
     */
    public static void demoPlusMinus() {
        LocalDateTime now = LocalDateTime.now();
        LocalDateTime future = now.plusDays(7).plusHours(2);
        LocalDateTime past = now.minusMonths(1).minusMinutes(30);

        System.out.println("Now: " + now);
        System.out.println("Next week +2h: " + future);
        System.out.println("1 month ago -30min: " + past);
    }

    /**
     * 比较时间先后
     * - 输出格式：布尔值或时间差（如天数）
     * - 数据库：不直接存储比较结果，用于业务逻辑判断
     * - 前端：用于显示过期提醒、倒计时等功能
     */
    public static void demoComparison() {
        LocalDateTime t1 = LocalDateTime.of(2025, 12, 31, 23, 59);
        LocalDateTime t2 = LocalDateTime.now();

        System.out.println("t1 is after t2? " + t1.isAfter(t2));
        System.out.println("t1 is before t2? " + t1.isBefore(t2));
        System.out.println("Difference in days: " + ChronoUnit.DAYS.between(t2, t1));
    }
}
```



## 🌳 生长思考

对发散的自由捕捉、精确化

## 💭 反复绊脚

记录回顾、使用文档时，遇到的困惑



## 🗺️ 修订记录

重要修订记录

## 🛠️ 实践经历

记录实践经历： demo + 工作经历 + 第三方优秀经验反思



## ⚙️ prompt

探究该文档模块过程中的 prompt 记录
模版如下：

```markdown
# 背景
我是一名追求效率的学习者。我从事于Java后端开发工程师的工作。
我认为学习本质上就是 大量正确信息的消化 + 关键结论的感受 + 自由的发散。
依托这样的思想，我希望和你来进行一场talk。

我们的 talk 基于以下原则
1. 你是 该talk 主题下的专家
2. 我是一名在其他领域具有通用性技能的工程师，如 Java、传统算法、后端工程、C++ 语法、Go 语法等。拥有一定计算机基础的知识。
3. 我们是不同领域的擅长者，这是一场圆桌会议式的talk。就和索尔维会议一样，阿尔伯特·爱因斯坦与尼尔斯·玻尔之间的交流。
4. 以你的权重为主前提下，我希望我们相互的问询感兴趣的内容。这样可以推动 talk 的进度。
# talk 主题
后端工程中的日期格式问题

# 思维链
1. 我对信息的要求是，你应该减少幻觉，即信息是足够正确的
2. 我总是对一件事物的是什么、从哪里来感到好奇。所以，你可以简略说明它的历史阶段以及发展哲学
3. 我一边和你 talk，一边做草稿笔记。因此你的言语足够开阔，具有阐述性的同时，也要让我容易从中记录归纳总结。但是请你不要直接给我总结笔记，因为我希望可以主动消化。

# 输入格式
我将会输入一个问题

# 输出格式
不需要特殊的模版。参照思维链
# 当前问题
我希望日期格式目前的主流规范是什么

# 问题阶段(记录了 talk 的过程，方便复用)


--------
以下内容是我方便复制粘贴模版，请你忽略
## 问题X：
### 问题描述：

### 关键结论：
```
