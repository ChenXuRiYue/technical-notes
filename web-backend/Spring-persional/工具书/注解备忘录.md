# 📝 注解备忘录

该文档记录一些常用、常忘的注解

## 🔖 接口层

⚙️ `PostMapping`

📖 **说明**

用于映射HTTP POST请求到处理方法，通常用于创建资源

💡 **使用方法**

```java
@PostMapping("/user")
public User createUser(@RequestBody User user) {
    return userService.save(user);
}
```

🪏 **经验**

| 场景 | 使用次数 |
| ---- | -------- |
|      |          |
|      |          |

🔗 引用

---

⚙️ `RequestBody`

📖 **说明**

用于绑定HTTP请求体到方法参数，通常用于接收JSON或XML格式的数据

💡 **使用方法**

```java
@PostMapping("/user")
public User createUser(@RequestBody User user) {
    return userService.save(user);
}
```

🪏 **经验**

| 场景 | 使用次数 |
| ---- | -------- |
|      |          |
|      |          |

🔗 引用

---

⚙️ `RequestParam`

📖 **说明**

用于绑定请求参数到方法参数，通常用于GET请求或表单提交

💡 **使用方法**

```java
@GetMapping("/user")
public User getUser(@RequestParam("id") Long userId) {
    return userService.findById(userId);
}
```

🪏 **经验**

| 场景 | 使用次数 |
| ---- | -------- |
|      |          |
|      |          |

🔗 引用

---

⚙️ `PathVariable`

📖 **说明**

用于绑定URL路径中的变量到方法参数，常用于RESTful风格接口

💡 **使用方法**

```java
@GetMapping("/user/{id}")
public User getUser(@PathVariable("id") Long userId) {
    return userService.findById(userId);
}
```

🪏 **经验**

| 场景 | 使用次数 |
| ---- | -------- |
|      |          |
|      |          |

🔗 引用

---

⚙️ `GetMapping`

📖 **说明**

用于映射HTTP GET请求到处理方法

💡 **使用方法**

```java
@GetMapping("/users")
public List<User> getAllUsers() {
    return userService.findAll();
}
```

🪏 **经验**

| 场景 | 使用次数 |
| ---- | -------- |
|      |          |
|      |          |

🔗 引用

---

⚙️ `RestController`

📖 **说明**

组合注解，相当于@Controller + @ResponseBody，用于创建RESTful风格的控制器

💡 **使用方法**

```java
@RestController
@RequestMapping("/api")
public class UserController {
    // 方法自动返回JSON
}
```

🪏 **经验**

| 场景 | 使用次数 |
| ---- | -------- |
|      |          |
|      |          |

🔗 引用

---

⚙️ `ControllerAdvice`

📖 **说明**

用于定义全局异常处理、全局数据绑定等，作用于所有控制器

💡 **使用方法**

```java
@ControllerAdvice
public class GlobalExceptionHandler {
    @ExceptionHandler(Exception.class)
    public ResponseEntity<String> handleException(Exception e) {
        return ResponseEntity.status(500).body(e.getMessage());
    }
}
```

🪏 **经验**

| 场景 | 使用次数 |
| ---- | -------- |
|      |          |
|      |          |

🔗 引用

---

⚙️ `ExceptionHandler`

📖 **说明**

用于定义异常处理方法，通常与@ControllerAdvice配合使用

💡 **使用方法**

```java
@ExceptionHandler(UserNotFoundException.class)
public ResponseEntity<String> handleUserNotFound(UserNotFoundException e) {
    return ResponseEntity.status(404).body(e.getMessage());
}
```

🪏 **经验**

| 场景 | 使用次数 |
| ---- | -------- |
|      |          |
|      |          |

🔗 引用

---

## 🔖 服务层

⚙️ `Service`

📖 **说明**

用于标识服务层组件，Spring会自动扫描并注册为Bean

💡 **使用方法**

```java
@Service
public class UserService {
    public User findById(Long id) {
        // 业务逻辑
    }
}
```

🪏 **经验**

| 场景 | 使用次数 |
| ---- | -------- |
|      |          |
|      |          |

🔗 引用

---

⚙️ `Transactional`

📖 **说明**

用于声明事务，确保方法在事务中执行，支持事务的传播行为和隔离级别

💡 **使用方法**

```java
@Transactional
public void transferMoney(Long fromId, Long toId, BigDecimal amount) {
    // 转账逻辑，要么全部成功，要么全部回滚
}
```

🪏 **经验**

| 场景 | 使用次数 |
| ---- | -------- |
|      |          |
|      |          |

🔗 引用

---

## 🔖 持久层

⚙️ `Repository`

📖 **说明**

用于标识数据访问层组件，Spring Data JPA会自动实现基本的CRUD操作

💡 **使用方法**

```java
@Repository
public interface UserRepository extends JpaRepository<User, Long> {
    User findByUsername(String username);
}
```

🪏 **经验**

| 场景 | 使用次数 |
| ---- | -------- |
|      |          |
|      |          |

🔗 引用

---

⚙️ `Mapper`

📖 **说明**

MyBatis的注解，用于标识Mapper接口，通常与@MapperScan配合使用

💡 **使用方法**

```java
@Mapper
public interface UserMapper {
    @Select("SELECT * FROM user WHERE id = #{id}")
    User findById(Long id);
}
```

🪏 **经验**

| 场景 | 使用次数 |
| ---- | -------- |
|      |          |
|      |          |

🔗 引用

---

## 🔖 配置层

⚙️ `Configuration`

📖 **说明**

用于标识配置类，相当于XML配置文件，可以定义Bean

💡 **使用方法**

```java
@Configuration
public class AppConfig {
    @Bean
    public UserService userService() {
        return new UserService();
    }
}
```

🪏 **经验**

| 场景 | 使用次数 |
| ---- | -------- |
|      |          |
|      |          |

🔗 引用

---

⚙️ `Bean`

📖 **说明**

用于声明一个方法返回的对象为Spring管理的Bean

💡 **使用方法**

```java
@Bean
public DataSource dataSource() {
    return new HikariDataSource();
}
```

🪏 **经验**

| 场景 | 使用次数 |
| ---- | -------- |
|      |          |
|      |          |

🔗 引用

---

⚙️ `Value`

📖 **说明**

用于注入配置文件中的属性值

💡 **使用方法**

```java
@Value("${app.name}")
private String appName;

@Value("${server.port:8080}")
private int serverPort;
```

🪏 **经验**

| 场景 | 使用次数 |
| ---- | -------- |
|      |          |
|      |          |

🔗 引用

---

⚙️ `Autowired`

📖 **说明**

用于自动装配Bean，可以标注在字段、构造器、方法上

💡 **使用方法**

```java
@Autowired
private UserService userService;

// 或构造器注入
@Autowired
public UserController(UserService userService) {
    this.userService = userService;
}
```

🪏 **经验**

| 场景 | 使用次数 |
| ---- | -------- |
|      |          |
|      |          |

🔗 引用

---

## 🔖 实体层

⚙️ `Entity`

📖 **说明**

JPA注解，用于标识实体类，对应数据库表

💡 **使用方法**

```java
@Entity
public class User {
    private Long id;
    private String name;
}
```

🪏 **经验**

| 场景 | 使用次数 |
| ---- | -------- |
|      |          |
|      |          |

🔗 引用

---

⚙️ `Table`

📖 **说明**

用于指定实体类对应的数据库表名

💡 **使用方法**

```java
@Entity
@Table(name = "t_user")
public class User {
}
```

🪏 **经验**

| 场景 | 使用次数 |
| ---- | -------- |
|      |          |
|      |          |

🔗 引用

---

⚙️ `Id`

📖 **说明**

用于标识实体类的主键字段

💡 **使用方法**

```java
@Id
private Long id;
```

🪏 **经验**

| 场景 | 使用次数 |
| ---- | -------- |
|      |          |
|      |          |

🔗 引用

---

⚙️ `GeneratedValue`

📖 **说明**

用于指定主键的生成策略，如自增、UUID等

💡 **使用方法**

```java
@Id
@GeneratedValue(strategy = GenerationType.IDENTITY)
private Long id;
```

🪏 **经验**

| 场景 | 使用次数 |
| ---- | -------- |
|      |          |
|      |          |

🔗 引用

---

⚙️ `Column`

📖 **说明**

用于指定字段对应的数据库列名及其他属性

💡 **使用方法**

```java
@Column(name = "user_name", nullable = false, length = 50)
private String username;
```

🪏 **经验**

| 场景 | 使用次数 |
| ---- | -------- |
|      |          |
|      |          |

🔗 引用

---

⚙️ `Data`

📖 **说明**

Lombok注解，自动生成getter、setter、toString、equals、hashCode方法

💡 **使用方法**

```java
@Data
public class User {
    private Long id;
    private String name;
}
```

🪏 **经验**

| 场景 | 使用次数 |
| ---- | -------- |
|      |          |
|      |          |

🔗 引用

---

⚙️ `Builder`

📖 **说明**

Lombok注解，自动生成建造者模式代码

💡 **使用方法**

```java
@Builder
public class User {
    private Long id;
    private String name;
}

// 使用
User user = User.builder()
    .id(1L)
    .name("张三")
    .build();
```

🪏 **经验**

| 场景 | 使用次数 |
| ---- | -------- |
|      |          |
|      |          |

🔗 引用

---

⚙️ `NoArgsConstructor`

📖 **说明**

Lombok注解，自动生成无参构造器

💡 **使用方法**

```java
@NoArgsConstructor
public class User {
    private Long id;
    private String name;
}
```

🪏 **经验**

| 场景 | 使用次数 |
| ---- | -------- |
|      |          |
|      |          |

🔗 引用

---

⚙️ `AllArgsConstructor`

📖 **说明**

Lombok注解，自动生成全参构造器

💡 **使用方法**

```java
@AllArgsConstructor
public class User {
    private Long id;
    private String name;
}
```

🪏 **经验**

| 场景 | 使用次数 |
| ---- | -------- |
|      |          |
|      |          |

🔗 引用

---

## 🔖 验证层

⚙️ `Valid`

📖 **说明**

用于触发参数验证，通常与@RequestBody配合使用

💡 **使用方法**

```java
@PostMapping("/user")
public User createUser(@Valid @RequestBody User user) {
    return userService.save(user);
}
```

🪏 **经验**

| 场景 | 使用次数 |
| ---- | -------- |
|      |          |
|      |          |

🔗 引用

---

⚙️ `NotNull`

📖 **说明**

验证字段不能为null

💡 **使用方法**

```java
@NotNull(message = "用户名不能为空")
private String username;
```

🪏 **经验**

| 场景 | 使用次数 |
| ---- | -------- |
|      |          |
|      |          |

🔗 引用

---

⚙️ `NotBlank`

📖 **说明**

验证字符串不能为null且不能为空字符串（去除空格后）

💡 **使用方法**

```java
@NotBlank(message = "用户名不能为空")
private String username;
```

🪏 **经验**

| 场景 | 使用次数 |
| ---- | -------- |
|      |          |
|      |          |

🔗 引用

---

⚙️ `NotEmpty`

📖 **说明**

验证集合、数组、Map等不能为null且不能为空

💡 **使用方法**

```java
@NotEmpty(message = "角色列表不能为空")
private List<String> roles;
```

🪏 **经验**

| 场景 | 使用次数 |
| ---- | -------- |
|      |          |
|      |          |

🔗 引用

---

⚙️ `Size`

📖 **说明**

验证字符串、集合、数组等的长度或大小

💡 **使用方法**

```java
@Size(min = 2, max = 20, message = "用户名长度必须在2-20之间")
private String username;
```

🪏 **经验**

| 场景 | 使用次数 |
| ---- | -------- |
|      |          |
|      |          |

🔗 引用

---

⚙️ `Min`

📖 **说明**

验证数值的最小值

💡 **使用方法**

```java
@Min(value = 18, message = "年龄必须大于等于18岁")
private Integer age;
```

🪏 **经验**

| 场景 | 使用次数 |
| ---- | -------- |
|      |          |
|      |          |

🔗 引用

---

⚙️ `Max`

📖 **说明**

验证数值的最大值

💡 **使用方法**

```java
@Max(value = 100, message = "分数不能超过100")
private Integer score;
```

🪏 **经验**

| 场景 | 使用次数 |
| ---- | -------- |
|      |          |
|      |          |

🔗 引用

---

⚙️ `Email`

📖 **说明**

验证字符串是否符合邮箱格式

💡 **使用方法**

```java
@Email(message = "邮箱格式不正确")
private String email;
```

🪏 **经验**

| 场景 | 使用次数 |
| ---- | -------- |
|      |          |
|      |          |

🔗 引用

---

⚙️ `Pattern`

📖 **说明**

使用正则表达式验证字符串格式

💡 **使用方法**

```java
@Pattern(regexp = "^1[3-9]\\d{9}$", message = "手机号格式不正确")
private String phone;
```

🪏 **经验**

| 场景 | 使用次数 |
| ---- | -------- |
|      |          |
|      |          |

🔗 引用

---

## 🔖 其他

⚙️ `Component`

📖 **说明**

通用的组件注解，用于标识Spring管理的Bean

💡 **使用方法**

```java
@Component
public class EmailUtil {
    public void sendEmail(String to, String subject, String content) {
        // 发送邮件逻辑
    }
}
```

🪏 **经验**

| 场景 | 使用次数 |
| ---- | -------- |
|      |          |
|      |          |

🔗 引用

---

⚙️ `Lazy`

📖 **说明**

用于延迟加载Bean，只有在第一次使用时才初始化

💡 **使用方法**

```java
@Lazy
@Autowired
private ExpensiveService expensiveService;
```

🪏 **经验**

| 场景 | 使用次数 |
| ---- | -------- |
|      |          |
|      |          |

🔗 引用

---

⚙️ `Scope`

📖 **说明**

用于指定Bean的作用域，如singleton、prototype等

💡 **使用方法**

```java
@Scope("prototype")
public class PrototypeBean {
    // 每次获取都会创建新实例
}
```

🪏 **经验**

| 场景 | 使用次数 |
| ---- | -------- |
|      |          |
|      |          |

🔗 引用

---

⚙️ `PostConstruct`

📖 **说明**

用于标注在Bean初始化后执行的方法

💡 **使用方法**

```java
@PostConstruct
public void init() {
    // 初始化逻辑
}
```

🪏 **经验**

| 场景 | 使用次数 |
| ---- | -------- |
|      |          |
|      |          |

🔗 引用

---

⚙️ `PreDestroy`

📖 **说明**

用于标注在Bean销毁前执行的方法

💡 **使用方法**

```java
@PreDestroy
public void destroy() {
    // 清理资源
}
```

🪏 **经验**

| 场景 | 使用次数 |
| ---- | -------- |
|      |          |
|      |          |

🔗 引用

---

⚙️ `ConditionalOnProperty`

📖 **说明**

根据配置属性的值决定是否创建Bean

💡 **使用方法**

```java
@ConditionalOnProperty(name = "app.cache.enabled", havingValue = "true")
@Bean
public CacheManager cacheManager() {
    return new RedisCacheManager();
}
```

🪏 **经验**

| 场景 | 使用次数 |
| ---- | -------- |
|      |          |
|      |          |

🔗 引用

