# 📝 常用代码积累

## 🔖 集合 

⚙️ **有限个元素构造集合**

- `Collections.singletonList("element")`
- `List<String> list = List.of("element");` jdk9+
- `new ArrayList<>(Arrays.asList("element"))`

🪏 **经验**

| 场景                     | 使用次数 |
| ------------------------ | -------- |
| 对单元素构造列表复用函数 | 2        |
|                          |          |

## 🔖 流操作



## 🔖 字符串

⚙️ **数组拼接字符串**

- _String原生：_`String s = String.join(",", arr);`  仅适用于 `String[] `

- _Stream：_ `String s = Arrays.stream(arr).collect(Collectors.joining(","));` 

  含 null 需先过滤/映射

- _Guava：_ `String s = Joiner.on(',').skipNulls().join(list);`

```java
String s = objs.stream().map(objs::getName)
                        .filter(Objects::nonNull)
                        .collect(Collectors.joining(","));

```

🪏 **经验**

| 场景               | 使用次数 |
| ------------------ | -------- |
| 对一数组构造字符串 | 1        |
|                    |          |

